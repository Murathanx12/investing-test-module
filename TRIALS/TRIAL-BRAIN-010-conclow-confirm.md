# TRIAL-BRAIN-010-conclow-confirm

**Registered:** 2026-07-25 (UTC) — BEFORE the confirm window is touched.
**Registry row:** `TRIALS/registry.jsonl`. **Grade:** the ONE pre-registered
confirm run earned by conc_low's batch-7 explore graduation (largemid
t_net 2.28, t_IC 4.46 — first graduate since gp-small).

## Hypothesis
Low customer concentration (diversified-customer suppliers, mirror of the
batch-6 book-inspected reversal) earns positive net excess return in the
LARGE/MID segment on the held-out confirm window 2019-01..2024-12 at
stress costs (50 bps one-way), replicating the explore direction.

## Provenance honesty
This candidate is a SEQUENTIAL MIRROR: cust_conc(+) was declared per
Patatoukas, rejected with IC t −7.4 (book-inspected clean), and the mirror
registered one batch later. The mining chain is disclosed; deflation uses the
FULL cumulative candidate count (N_TRIALS = 140). Internal control recorded:
the + direction lost −21 bps on the identical coverage universe, so the
±spread is not a coverage-selection artifact.

## Frozen spec
`scan_signal` mechanics verbatim (top decile, 30% hold-band), segment
largemid, window 2019-01..2024-12, cost 50 bps one-way (stress), signal =
batch-7 `conc_low` construction byte-identical. ONE run. Results final.
Reruns forbidden; variants are new trials.

## Mandatory pre-confirm book inspection (BEFORE any return is read)
Top-of-book composition on 3 confirm-window formation months: concentration
values, size (dollar-vol rank), and link-vintage staleness. Abort (VOID, not
REJECT) on any adjustment/staleness artifact signature — the tgt_upside rule.

## Decision rule (frozen, mirrors BRAIN-008)
- **PASS:** confirm net excess > 0 AND t_net ≥ 0.8 AND confirm IC > 0.
  DSR at N_TRIALS=140 reported honestly (expected weak) — PASS earns
  survivor status with the DSR caveat disclosed, exactly like BRAIN-008.
- **REJECT:** anything else → NEGATIVE_RESULTS; the conc family closes
  (both directions then adjudicated).

## Result (filled AFTER the run — never edited)
_pending_
