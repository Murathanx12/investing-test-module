# Session log — 2026-07-19 (module bootstrap)

## Done
1. **ROADMAP.md** — synthesized the five AI audits + repo survey into the layered/firewalled
   Investor Brain architecture (L0–L5), hard constraints, phases, literature priors.
2. **EODHD archive backed up** (closeout item #1): `OneDrive\AegisBackups\
   eodhd_archive_2026-07-19.tar`, 1.07 GB, 50,471 entries verified.
3. **aegis_brain package** (commits e7986ca, 65c2d1b): vendored discipline math
   (DSR/PSR/PBO/purged-CV from aegis-finance@790a0cb), delisting-aware EODHD panel loader,
   GKX baseline signals, shallow GBM/MLP rankers, expanding-window walk-forward harness
   (structural 1-month embargo, one-way costs on traded value), adoption gate + append-only
   local trial registry (deflates vs 14 main-repo base). 17 tests green, incl. planted-alpha
   detection, noise control, no-leak design check, real-archive smoke.
4. **Panel cache built**: 33,445 symbols × 115 months (2017-01→2026-07), 16,796 in-window
   deaths, ~5,200 eligible names/month. `data/panel_2017/` (gitignored).
5. **TRIAL-BRAIN-000-baseline** pre-registered (cumulative → 15) and executed once:
   **REJECT** — surfaced catastrophic OTC/pink adjusted-close corruption (5,938 obs
   >1000%/mo, max 3.3e9). Led to the clean universe DEFINITION: `list_clean_symbols`
   (NYSE/NASDAQ/AMEX/ARCA/BATS common stock via symbol-list metadata) + impossible-return
   nulling. Clean panel: 12,419 names, 6,701 in-window deaths, ~4,500 eligible/month.
6. **TRIAL-BRAIN-001-baseline-clean** pre-registered (cumulative → 16) and executed once:
   **REJECT on the merits** — GKX big-three via shallow GBM does not beat universe EW net
   of 25 bps (Arm B t = −1.40; DSR 0.124). Two structural lessons: (a) **turnover is the
   killer** — monthly-refit decile books trade ~180%/mo one-way = 45 bps/mo drag; every
   future candidate needs turnover-aware construction; (b) the leak control must be
   stated on GROSS excess (harness now reports `t_stat_excess_gross`; noise gross t = 1.19
   confirmed the harness unbiased). Full narratives in the trial docs — both stand as
   published negatives.

## Notes / gotchas
- A PowerShell background job died silently at ~51% of the panel build while reporting
  exit 0 — rebuilt in foreground successfully. Distrust background exit codes for
  long PowerShell jobs; prefer foreground with a long timeout or Bash.
- `requirements.txt` gained `pyarrow>=15.0` for the parquet cache.

## Still owed (Murat, from V5 closeout)
- EODHD auto-renew OFF; QuantConnect 3-lane Share URLs; live-site click-through;
  chase HKU WRDS.

## Monday 2026-07-20 (Phase 0, main repo — do FIRST next session)
- Alpaca: did DKNG 1,897 + SLDP 22,500 fill at the open? `alpaca:equity` divergence snapshot.
- Congress collector: real data at 07:30 ET? Watch for false-zero poisoning.
