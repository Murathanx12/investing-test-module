# Adversarial pipeline audit — 2026-07-21 (disposition)

A skeptical red-team read of the whole `aegis_brain` pipeline (crsp_panel, runner,
overfitting math, purged CV, insider collector, ledger, realized). **Bottom line: the core
walk-forward path is clean** — no look-ahead in the CRSP+runner timeline, delisting captured
correctly, costs/benchmark conservative (biased against the strategy), DSR/PSR/PBO math
faithful to Bailey-López de Prado, insider classifier point-in-time, ledger registration
anti-look-ahead. Findings and what we did:

| ID | Sev | Finding | Disposition |
|---|---|---|---|
| H1 | HIGH | CRSP panel lacked impossible-return / sentinel hygiene (EODHD had it); 1 stray +2658% return present | **FIXED** — RET_FLOOR/CAP guard + sentinel coercion in `build_crsp_panel`; panel rebuilt from raw (nulled 1). |
| M2 | MED | DSR under-counted trials (4-6 configs/trial not counted) + fixed low sr_variance → biased toward passing | **FIXED for BRAIN-003** — n_trials = cumulative + n_configs (24); revisit sr_variance batch estimate. |
| M3 | MED | PBO never computed (perf_matrix never passed) → gate inert, DSR decorative | **FIXED for BRAIN-003** — PBO computed across the 6-config batch (0.41). |
| M4 | MED | Runner filled NaN forward returns to 0 without renormalizing → silent P&L misstatement | **FIXED in BRAIN-003 runner** (drop+renormalize+log count); port to generic runner next. |
| M5 | MED | Ledger realized-return aligned stock vs SPY positionally, not on shared sessions → misaligns around event halts | **FIXED** — intersect trading dates in `realized.py`. |
| M6 | MED | PurgedKFold post-test embargo < label horizon (latent; not wired into walk-forward) | **DEFERRED** — fix before any crash/forward-window label uses it. |
| M7 | MED | Insider signal must stamp at filing_date not trans_date (latent) | **HONORED** — BRAIN-003 flag built from filing_date. |
| — | insider parser | Malformed SEC year (e.g. "0013") crashed the whole quarter | **FIXED** — sane-year guard + coerce; recovered 3 lost quarters. |
| L8-L13 | LOW | maturity() proxy wording, LS benchmark, "12-1" label = 11mo, EODHD collision tilt, multi-owner attribution | Documented; low impact. Address opportunistically. |

Deferred items (M6, generic-runner M4 port, sr_variance batch estimate, L-series) are tracked
here and in the code comments; none block BRAIN-003, whose result is reported with these
fixes applied.
